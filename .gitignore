node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
npm-debug.log*
.env
.env.*
!.env.example

# Firebase Emulator Suite (Phase 2 PR 1): local-only logs and downloaded emulator binaries.
firebase-debug.log
firestore-debug.log
database-debug.log
ui-debug.log
.firebase/

# Vitest JSON reporter output (never committed; trips prettier --check otherwise).
.vitest/

# Eat the Reich (B01/B05, docs/ETR_RULES_MATRIX.md §5): a GM's own private,
# licensed rulebook content pack, loaded locally only, never committed.
content/private/
