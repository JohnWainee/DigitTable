{
  "name": "@digitable/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Platform authorization, deterministic randomness, and the pure command-run harness shared by every template.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@digitable/contracts": "*"
  },
  "devDependencies": {
    "@digitable/testing": "*",
    "fast-check": "^3.23.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
