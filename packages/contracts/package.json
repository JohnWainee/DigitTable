{
  "name": "@digitable/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Wire schemas, IDs, stable errors, and the platform/template contract types shared by the engine and templates.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "fast-check": "^3.23.1",
    "typescript": "^5.6.3",
    "vitest": "^5.0.0"
  }
}
