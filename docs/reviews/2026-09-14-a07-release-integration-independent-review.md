# A07 (partial) — region co-location fix + operations runbook — independent review

- **PR:** #32 (`sonnet-a/a07`, based on `sonnet-a/a06` / PR #30, stacked on #27/#23/#18/#15/#13)
- **Reviewed commit:** `ef15eb2`
- **Reviewer:** two independent read-only review passes, dedicated worktree `.claude/worktrees/sonnet-a-a07`, no code changes made by either (the first pass stalled mid-run on a long-running background command and was re-run in the foreground to completion per the coordinator's direction; both passes that returned a result reached the same conclusion)
- **Scope:** `packages/contracts/src/deployment.ts` (new `FUNCTIONS_REGION` constant) applied to all 5 callables and the web client's `getFunctions`, plus `docs/RUNBOOK.md`

## Verdict

**No blocking findings.** The region fix is complete, correctly scoped, and empirically verified against the emulator's own registered function names (not just a passing test count — the reviewer specifically checked for this, since the local emulator would route a region mismatch without failing). The runbook is accurate against the actual source it cites and does not overclaim readiness anywhere checked.

## Findings

1. **Region fix — consumed on both sides, nothing missed (PASS).** `FUNCTIONS_REGION = "us-west1"` is applied to `CallableOptions.region` on all 5 `onCall`s (`admitMember`, `claimSeat`, `createRoom`, `recoverSeat`, `submitRoomCommand`) and to the client's sole `getFunctions(app, FUNCTIONS_REGION)` call site. Grepped for every `onCall`/`getFunctions` call site in both `apps/functions/src` and `apps/web/src` — none exist outside the files already touched.
2. **`firebase.json` needs no additional region setting (PASS).** Verified against `firebase-functions`' actual `HttpsOptions`/`CallableOptions` type, not just the code comment — per-callable `region` is the correct and sufficient mechanism for 2nd-gen functions; `firebase.json`'s `functions` block correctly carries no region key.
3. **Empirical proof, not just a passing count (PASS).** Raw emulator output registers all five functions under region-qualified names (`functions[us-west1-admitMember]`, etc.) and the test run's own trace shows `Beginning execution of "us-west1-createRoom"` / `"us-west1-admitMember"` / `"us-west1-submitRoomCommand"` — real evidence the client and server names now agree, which a passing test count alone would not have proven (the emulator routes by name regardless of region correctness).
4. **No behavior regression (PASS).** `git diff sonnet-a/a06..sonnet-a/a07 -- apps/functions/src/callables.ts apps/functions/src/gameCallables.ts` shows only the new `FUNCTIONS_REGION` import, `region: FUNCTIONS_REGION` added to each `CallableOptions`, and Prettier-forced reformatting from the options object growing a key. No transaction logic, authorization ordering, or handler body changed.
5. **Runbook accuracy — session setup, resume/recovery (PASS).** Spot-checked against `createRoomAuthority.ts`, `admissionAuthority.ts`, `gameCommandAuthority.ts`, `recoverySeatAuthority.ts`, and `throttle.ts` — every specific claim (requestId idempotency, server-echoed `roomRevision`, table-code isolation, bounded no-early-exit recovery scan, atomic rebind, fresh replacement code, GM-visible leak-safe audit entry, `RECOVERY_THROTTLE_LIMITS` numbers, R3/R4/R5 residual language) matches the real source and `docs/ARCHITECTURE.md` verbatim in substance.
6. **Runbook accuracy — section 8's fixture-engine claim (Low, fixed).** The fixture-engine files the runbook names genuinely exist, but only on Sonnet C's unmerged branches (confirmed via `git ls-tree -r sonnet-c/c06-integration`), not anywhere in `sonnet-a/a07`'s own `apps/web/src` tree — which is still the older, unrelated Phase 1A/1C local-role-simulation harness (`InMemoryRoomRepository`) and never reached the fixture-engine stage at all. The conclusion (production screens aren't wired to the real backend, rehearsal is honestly blocked) is correct either way; only the framing was imprecise about whose tree the cited files live in. **Fixed**: section 8 now states plainly that this branch's own `apps/web` is a separate, more primitive stub, and that the cited fixture-engine files are cross-track information about Sonnet C's branches specifically (confirmed via `git ls-tree`), not part of this checkout.
7. **No overclaiming found (PASS).** Every reference to a real deploy, backup, or rehearsal is explicitly framed as not-yet-done; the deploy log section is empty with instructions never to overwrite it; the GitHub PR description matches this framing precisely.
8. **Judgment on deferring the full rehearsal (agree with the call).** Given the confirmed blocker (Sonnet C's actual production screens are fixture-wired on a separate, unmerged, in-progress branch), there was no honest way to produce three-device rehearsal evidence without either fabricating it against a fixture engine that would validate nothing about this PR's actual backend, or reaching across tracks into Sonnet C's unfinished, unowned integration work. Deferring with a specific, named blocker is the right call.

## Verification (re-run after the section 8 fix)

- `npm run check`: PASS — 325/325 tests, 43 files, format/lint/typecheck clean (unaffected by a documentation-only change).
- `npm run build`: PASS — functions bundle 100.2kb, web build 80 modules.
- `PATH=/opt/homebrew/opt/openjdk/bin:$PATH npm run test:emulator`: PASS — **106/106** (17 `packages/testing` + 86 `apps/functions` + 3 `apps/web`), with the emulator log confirming `us-west1`-qualified function names executing, not just test counts.
