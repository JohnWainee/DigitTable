# Sixth independent review: mobile pop-out defect and full reskin control matrix (branch `sonnet-ah/reskin-closure-20260925`, 2026-09-25)

**Scope.** A fresh, isolated Sonnet reviewer, starting from `factory/today-integration` at
`e7efe29` (docs-only, records the fifth independent review), was asked to independently audit the
ink-black/near-black punk-zine visual hierarchy and every select, menu, disclosure, modal, drawer,
option list, and allocation/action picker across phone, tablet, desktop, and table layouts —
visual-viewport changes, keyboard, safe areas, no page zoom/horizontal overflow, 44px touch
targets, internal scrolling, focus behavior, reduced motion, WCAG accessibility, and all
GM/player/table flows — and to fix any concrete defect found or otherwise record a rigorous
closure review, without cosmetic churn. This is the sixth independent pass over this exact defect
area, following
[`2026-09-24-sonnet-w-utility-item-tap-target-review.md`](2026-09-24-sonnet-w-utility-item-tap-target-review.md),
[`2026-09-25-sonnet-w-ui-audit-roster-selector-review.md`](2026-09-25-sonnet-w-ui-audit-roster-selector-review.md),
[`2026-09-25-sonnet-y-reskin-integration-independent-review.md`](2026-09-25-sonnet-y-reskin-integration-independent-review.md),
[`2026-09-25-sonnet-ad-mobile-popout-independent-review.md`](2026-09-25-sonnet-ad-mobile-popout-independent-review.md),
and
[`2026-09-25-sonnet-af-mobile-popout-independent-review.md`](2026-09-25-sonnet-af-mobile-popout-independent-review.md).

**Method.** Read `AGENTS.md`, `CLAUDE_HANDOFF.md` (in full — both halves of the file), and
`docs/UX_RESOLUTION_THEATRE.md` before touching anything. Confirmed `git log` shows this worktree
sits exactly at `factory/today-integration`'s `e7efe29` with a clean tree.

Independently re-derived the control inventory rather than trusting prior summaries: grepped every
`apps/web/src/**/*.tsx` for `<select`, `<details`, `<summary`, `SheetDialog`, `role="dialog"`,
`role="menu"`, `role="listbox"`, and `<dialog`, and separately every file containing `onClick=`.
Found the identical set every prior pass recorded — six native `<select>`s (`SceneDirector.tsx`
x2, `GmToolsPanel.tsx` x4), one `<details>`/`<summary>` ("Why?" in `ComposeStep2.tsx`), and one
`SheetDialog` consumer (`CorrectionDialog.tsx`) — with no new pop-out-shaped control introduced
anywhere in `apps/web/src`. Then, rather than only checking the two previously-fixed buttons, read
every `<button` occurrence (with surrounding context) in all 19 files that use `onClick=`
(`CorrectionDialog.tsx`, `GmDirectorScreen.tsx`, `GmSeatRequired.tsx`, `GmToolsPanel.tsx`,
`PendingActionsPanel.tsx`, `RosterPanel.tsx`, `SceneDirector.tsx`, `ClaimCharacterScreen.tsx`,
`CreateSessionScreen.tsx`, `JoinScreen.tsx`, `JoinTableScreen.tsx`, `LandingScreen.tsx`,
`AllocationPanel2.tsx`, `ChooseInjuryPanel2.tsx`, `ComposeStep2.tsx`, `ConfirmSummary2.tsx`,
`PlayerDashboardScreen.tsx`, `AllocationStepper.tsx`, `TableDashboardScreen.tsx`) and confirmed
every multi-line `<button` element carries a `className` (`primary-action`, `secondary-action`,
`link-button`, or a `.stepper-controls` descendant) — no class-less button exists outside the one
documented `.stepper-controls` exception. This directly re-verifies, by source reading rather than
by trusting the static test, that the class of defect `sonnet-w`'s pass found and fixed
(`5663d7f`) has not recurred anywhere else in the app.

**Gates run this session** (fresh worktree, fresh `npm install` — no `node_modules` existed
before this session):

- `npm run check` — **692 tests passed | 11 todo** (71 files passed, 1 skipped), format/lint (zero
  warnings)/typecheck clean. Matches every prior pass's recorded baseline exactly.
- `npm run build` — clean (`apps/functions` esbuild 216.6kb; `apps/web` vite build, 138 modules;
  the existing non-blocking >500kB chunk warning, unchanged).
- `PATH=/opt/homebrew/opt/openjdk/bin:$PATH npm run test:emulator` — **108/108** (18
  `packages/testing`, 86 `apps/functions`, 4 `apps/web`) against the local `demo-digitable`
  Auth/Firestore/Database/Functions emulators.
- `git diff --check` — clean; the only working-tree change from the fresh `npm install`
  (immaterial `package-lock.json` `"peer": true` metadata churn, the same diff every prior review
  in this series discarded) was reverted with `git checkout -- package-lock.json`, not committed.

**Fresh live evidence generated by this session, with screenshots (not just cited).** Built a
from-scratch emulator-mode production bundle (`VITE_FIREBASE_USE_EMULATOR=true` plus a fake
`demo-api-key`/`demo-digitable.firebaseapp.com`/`demo-digitable`/dummy-app-id config — no real
Firebase project, credential, or resource touched), started the local Auth/Firestore/Database/
Functions emulators (`demo-digitable`, `127.0.0.1`-bound ports 9099/8080/9000/5001, confirmed all
four reporting "All emulators ready" before proceeding), served the build via `vite preview` on
`127.0.0.1:4174` (no LAN exposure), and ran, **with screenshots enabled** (prior passes used
`--no-shots`; this pass kept the default so visual before/current evidence exists for this
review):

```
node scripts/playtest/ui-audit.mjs --base http://127.0.0.1:4174 \
  --label sonnet-ah-independent --out /tmp/sonnet-ah-ui-audit --port 9766
```

Result: `ok: true`, **150 states**, **1,422 controls audited**, **0 control issues**, **0 overflow
states**, **0 hard axe violations**, **0 failures**, **14/14 modal/sheet scenarios** (phone-small
(320)/phone(375)/phone-landscape(812×375 and 667×375)/tablet(768)/desktop(1280) widths, plus
dynamic-viewport-keyboard emulation, real-Chrome pinch-zoom, emulated safe-area insets, 150%/200%
text scaling, internal scroll-to-control, and reduced-motion). Independently parsed the raw
`report.json` (not just the printed summary) with a small Node script: every modal scenario's
`checks` object reads all-`true` except the one already-documented `text-200-phone-small` extreme
(`dialogInsideViewport`/`noPageOverflow` both `false`, logged by the script itself as
`INFO ... (recorded, not gating)`) — matching every one of the five prior reviews' accepted
geometry limit exactly, not a new finding. `reducedMotion` capture: `sheetDuration` `0.18s`
(allowed) vs `1e-06s` (reduced), `anyLongTransition` `true` vs `false` — the sheet's rise/fade
animation genuinely collapses under `prefers-reduced-motion: reduce`. `console` capture: zero
console errors and zero failed requests across all four surfaces (`gm`, `player`, `table`,
`anon`). The one `axeBestPractice` entry
(`anon/route-claim-no-such-room@phone: page-has-heading-one`) is the same pre-existing,
non-hard-violation note on the intentional nonexistent-room route every prior review recorded.

**Visual inspection of captured screenshots** (this pass's own addition — prior five passes ran
`--no-shots`): reviewed `anon-landing-phone.jpg` (ink-black background, high-contrast yellow/red/
cyan punk-zine buttons and headline treatment, legible at 375px), `gm-correction-sheet-phone.jpg`
(the `SheetDialog` pop-out at phone width — sticky footer with "Apply correction"/"Cancel" fully
reachable and tap-sized, scrollable body), `gm-correction-text-200-phone-small.jpg` (the one
accepted non-gating extreme — confirms visually it is a minor top/bottom clipping at 320px+200%
text, not a broken or unusable control), `gm-console-pending-desktop.jpg` and
`gm-console-pending-table.jpg` (all four `GmToolsPanel` `<select>`s and the `SceneDirector`
`<select>`s render correctly with visible focus/border treatment at both widths, no overflow),
`player-allocation-phone.jpg` (the allocation picker's radio targets are large and legible at
375px), and `player-compose-why-open-phone-small.jpg` (the "Why?" `<details>` disclosure expanded
at the smallest supported phone width, 320px — the previously-fixed `link-button` "Mark and regain
Blood" utility-item control is present, legible, and visually distinct as a tap target). No
rendering defect, clipped control, or overflow visible in any inspected screenshot beyond the one
already-accepted extreme.

**GM/player/table state semantics.** Re-confirmed by direct source reading: `GmToolsPanel.tsx` and
`SceneDirector.tsx` (all six `<select>`s and every GM-only command form) are imported and rendered
only from `GmDirectorScreen.tsx` (`#/room/:roomId/gm`); no player- or table-surface component
imports either file. This is unchanged and was not weakened by anything read this session.

**No in-scope defect found; no code change made.** The pop-out system (`SheetDialog`, the six
native `<select>`s, the one `<details>` disclosure, the custom `role="spinbutton"` allocation
stepper) and every button-based control in the app are confirmed — by fresh, from-this-session
source reading, gate reproduction, and live-browser evidence including inspected screenshots, not
by citing prior runs — to satisfy every geometry, 44px+ tap-target, focus-trap, scroll-lock,
safe-area, and reduced-motion invariant this audit checks, at phone/tablet/desktop/table widths,
under dynamic-viewport-keyboard emulation, pinch-zoom, safe-area insets, and 150%/200% text
scaling. Per the task's explicit instruction not to make cosmetic churn when prior rounds passed,
no speculative change was made. Functional semantics, privacy/authorization invariants (platform
authorization before template authorization, per-viewer projection isolation, hidden-field
redaction, GM-only tool reachability), and sourcebook/provenance constraints are all unaffected —
nothing in `packages/*`, `templates/*`, `apps/functions`, `firestore.rules`,
`database.rules.json`, or the engine/authorization boundary was touched or needed to be. This is a
documentation-only entry (this file plus the `CLAUDE_HANDOFF.md` note it is linked from); no merge,
deploy, resource creation, or promotion occurred. Background emulator/preview processes were
stopped after the run; nothing was committed from the emulator-mode build (`dist/` is gitignored);
the `package-lock.json` churn from the fresh install was discarded.

**Physical-device limitation (unchanged, explicitly noted, not reopened as a defect).** This
review, like all five prior passes, ran entirely in real-Chrome headless CDP emulation on this
machine — real iOS/Android hardware, real on-screen-keyboard resize behavior, and real Safari
visual-viewport quirks were not and could not be exercised here. That gap remains open for John
under `docs/PLAYTEST_TWO_DEVICE.md` and `CLAUDE_HANDOFF.md`'s physical-device evidence item; this
review does not close it and does not claim to.
