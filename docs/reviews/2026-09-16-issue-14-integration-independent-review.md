# Issue #14 integration candidate — independent review

- **PR:** #38 (`claude/issue-14-resume-cap2zr`, based on `main` @ `c5ad236`, the post-#18 merge)
- **Reviewed commit:** `61059c1`
- **Reviewer:** one independent read-only pass by a fresh reviewer agent given only the branch, the base, the list of merged branches, the four conflicted files, and `AGENTS.md`'s invariants — not the author, no prior context from the integration session, no code changes made
- **Scope:** the integration itself — the four merge commits (`e7c7a88` a08-final, `8246b13` b05-fixtures-review, `9ad8442` c08-recovery, `4b28872` fable/etr-specifications), their conflict resolutions, and the one fix commit on top. Not the merits of each track's slices, which carry their own review records under this directory.

## Verdict

**Approve; no blocking findings.** The four merges lose nothing: every file each branch changed relative to its merge-base with `main` is either byte-identical on the head or differs only because a later-merged branch or a stated conflict resolution supersedes it, and each such difference was checked individually. `packages/contracts/src/errors.ts` is the exact union of all five branch versions (28 codes) and `apps/functions/src/httpsErrors.ts`'s `grpcCodeFor` stays exhaustive over it under `tsc --noEmit`. No placeholder-engine identifier survives outside `docs/`. The fix commit touches exactly the five files claimed; its rewritten emulator suites preserve every assertion of the A04 originals, add redaction and GM-only checks, and every baseline-relative revision/sequence number is correct against `runCommand`'s one-sequence-per-decided-event rule. `npm run check` reproduced at 449 passed / 11 todo / 47 files + 1 skipped. All 21 open per-track PR heads (#16, #17, #19–#37) are ancestors of the head, as the PR claims. One Low finding was fixed on the branch after the review; the other two are informational and pre-existing.

## Findings

1. **`gameCommand.test.ts` — test title claimed rejected-command log coverage the body never exercised (Low, pre-existing, fixed).** "never logs the random seed, the payload, or a rejected command's underlying state" only submitted an accepted `BeginAction`; nothing checked a rejected path's log line, and the seed clause is untestable as written (a fixed seed byte serialises as a bare digit). The A04 original had the same shape (asserted only the placeholder action id), so this was not an integration regression; the fix commit had already strengthened it to two negative assertions. **Resolution:** the test now also submits an unknown command carrying the private note, asserts the rejection, asserts the serialised log contains neither the note, the stat, nor the unknown command type, and asserts exactly one `accepted` and one `rejected` `gameCommand.result` line in that order; the title now claims only what the body proves. Re-verified with the emulator suite (below).
2. **Determinism test does not exercise retry contention (Info, pre-existing, no change).** The title's "regardless of which internal attempt commits" is stronger than the single committed attempt per room the test runs. The property it does prove is valid: the seed is drawn once outside the transaction and the seeded generator is built inside it; `decideReviewAction` draws the player pool then the attack dice from that generator; the pool size is a pure function of Rook's SNEAK stat with no items, abilities, or engaged Threats, so two rooms with the same seed byte, roster, scene, claim and declaration are comparable; `ActionRolled` is broadcast to `shared` only, so `sharedEvents[0].playerFaces` is real data. Same limitation as the A04 original.
3. **Future-dated review record inherited from B05 (Info, out of scope).** `docs/reviews/2026-09-17-etr-rules-independent-review.md` is dated one day after the review date. It comes from `origin/sonnet-b/b05-fixtures-review`, not the integration; noted so the filename can be corrected in a docs pass.

Nothing else found: no dropped code, no wrong conflict resolution, no vacuously-passing test, no stray or ignored artifacts committed.

## Verification checklist (reviewer's own runs)

- **A. Nothing lost in the merges — PASS.** For each branch, every file in `git diff --name-only origin/main...origin/<branch>` compared against the head: a08-final differs only in the handoff, the two rewritten emulator tests, and `FirebaseSessionClient.ts` (C08's added `recoverSeat` method; head equals C08); b05 differs only in `.gitignore` (union), `errors.ts` (union), `engine.ts` (see B), and `test/fixtures.ts` (C08's added authority fields; head equals C08); c08 differs only in the `apps/functions` sources, `admission.test.ts`, `apps/web/src/firebase/functions.ts`, `package.json` and `packages/contracts/src/index.ts`, each byte-equal to a08-final, and `git log --no-merges` shows C authored nothing in those paths, so A07/A08 legitimately supersede; fable and errors-for-b have no differences. `firebase.json`, `firestore.rules`, `package-lock.json` were identical on A08 and C08. All five branch tips are ancestors of the head.
- **B. Conflict resolutions correct — PASS.** `engine.ts` differs from B05 only by C's `initialState` doc comment and the cast removal, and from C08 only by the cast removal. Grep for `PLACEHOLDER_`, `placeholderCharacter`, `MAX_PUSH_DICE`, `strong-arm` across `*.ts`/`*.tsx` outside `docs/` is empty; remaining `actorMemberId`/`gearIds`/`threatId` hits are B's real `RollRecord`/`RevealThreat` fields and the contracts `PoolInput`.
- **C. Fix commit — PASS.** `git show 61059c1 --stat` is exactly the five files stated. Test inventory of `gameCommand.test.ts` went from 17 to 19 with all 17 originals present by title and assertion content. Baseline arithmetic checked against `runCommand.ts` (one sequence per decided event, shared by its effects), `decideBeginAction` (one event, three destination copies) and `gameCommandAuthority.ts`'s `events/{partition}/items/{sequence}` write. Cross-player allocation still proves ownership because `decideAllocateResults` checks the actor before the roll status. `session.test.ts`'s revisions 1/2/3 are right because `createRoom` writes `roomRevision: 0` and admission never bumps it; the GM repository is bound to the app that created the room. `initialState.test.ts` matches the real `initialState`; the pre-fix merged tree (and C08 itself) still carried A03's `expect(state.characters).toEqual({})` against B's engine, so the fix was necessary. `engine.ts`'s change is the import and cast removal only; no remaining `StableErrorCode` use.
- **D. Repo invariants — PASS.** `npx vitest run templates/eat-the-reich packages/contracts packages/engine`: 35 files passed, 1 skipped (`rulesMatrix.todo.test.ts`, from B02), 396 passed / 11 todo, including the projection-isolation properties, budget fixtures, schema round-trips and platform-authorization tests. Typecheck clean for `apps/functions`, `apps/web`, `templates/eat-the-reich`, `packages/contracts`, `packages/engine`; eslint and prettier clean on the touched files. Full `npm run check`: 449 passed / 11 todo, 47 files + 1 skipped.
- **E. Docs consistency — PASS.** The handoff's integration section matches the merge diffs and the fix commit's file list; the PR body matches the handoff; `mergeable_state: clean`. The emulator count and `npm run build` were not re-run by the reviewer.

## Verification after the finding-1 fix (author, this branch head)

- `npx prettier --check`, `npx eslint`, `npm run typecheck -w @digitable/functions` on the changed test: clean.
- `npm run test:emulator` (proxy variables unset for the localhost-only subprocess, see the handoff's environment note): **108/108** (17 `packages/testing` + 88 `apps/functions` + 3 `apps/web`), unchanged count, the strengthened test passing.
