# Fourth independent review: mobile pop-out defect (branch `sonnet-ad/reskin-hourly-20260925`, 2026-09-25)

**Scope.** A fresh, isolated Sonnet reviewer (no access to prior sessions' reasoning) was asked
to audit every select, menu, disclosure, modal, drawer, option list, and action/allocation
picker at real small-phone, landscape, tablet, desktop, and table viewport configurations —
including visual-viewport changes from browser chrome/on-screen keyboard, safe areas,
zoom/text-scaling, touch, and keyboard — starting from `factory/today-integration` at commit
`d5f9bce` (merge: integrate reviewed mobile reskin audit fixes), the base that folds in the
three prior independent passes on this defect
([`2026-09-24-sonnet-w-utility-item-tap-target-review.md`](2026-09-24-sonnet-w-utility-item-tap-target-review.md),
[`2026-09-25-sonnet-w-ui-audit-roster-selector-review.md`](2026-09-25-sonnet-w-ui-audit-roster-selector-review.md),
[`2026-09-25-sonnet-y-reskin-integration-independent-review.md`](2026-09-25-sonnet-y-reskin-integration-independent-review.md)).

**Method.** Read `AGENTS.md`, `CLAUDE_HANDOFF.md`, `docs/ARCHITECTURE.md`,
`docs/IMPLEMENTATION_ROADMAP.md`, `docs/TEMPLATE_ARCHITECTURE.md`,
`docs/DATA_AND_SYNC_MODEL.md`, `docs/UX_RESOLUTION_THEATRE.md`,
`docs/EAT_THE_REICH_BUILD_GUIDE.md`, `docs/PLAYTEST_TWO_DEVICE.md`, and every reskin-related
`docs/reviews/` record before touching anything. Diffed `930e3f1` (the last fully independently
reviewed reskin evidence) against `d5f9bce`: `apps/web/src/styles.css`, `apps/web/src/shared/**`,
and `apps/web/index.html` are still byte-for-byte unchanged — the pop-out system itself
(`SheetDialog`, the six native `<select>`s, the one `<details>` disclosure) carries no new
source change. The only files that changed since `930e3f1` and are relevant to this defect are
`apps/web/src/player2/ChooseInjuryPanel2.tsx`, `apps/web/src/player2/ComposeStep2.tsx` (the
utility-item tap-target fix), `apps/web/src/player2/PlayerDashboardScreen.tsx` (wiring for that
fix — one new handler, two new optional props threaded through, no new interactive elements of
its own), `apps/web/test/styles/reskinContract.test.ts` (the new class-less-button regression
test), and `scripts/playtest/ui-audit.mjs` (the roster-agnostic correction-button selector).
Independently confirmed both fixed buttons in `ChooseInjuryPanel2.tsx`/`ComposeStep2.tsx` carry
`className="link-button"` at `d5f9bce` (grep, not citation), and re-grepped every `.tsx` file
under `apps/web/src` for pop-out-shaped markup (`<select`, `<details`, `<dialog`,
`role="menu"`, `role="listbox"`, `SheetDialog`, `popover`) — the same five files the prior
reviews found (`CorrectionDialog.tsx`, `GmToolsPanel.tsx`, `SceneDirector.tsx`,
`ComposeStep2.tsx`, `SheetDialog.tsx`), none of which changed since `930e3f1`.

**Gates reproduced this session** (fresh `npm install`, no reuse of any prior worktree's
`node_modules` or build output):

- `npm run check` — **692 tests passed | 11 todo** (71 files, 1 skipped), format/lint (zero
  warnings)/typecheck clean.
- `npm run build` — clean (`apps/functions` esbuild 216.6kb; `apps/web` vite build, 138 modules;
  existing non-blocking >500kB chunk warning unchanged).
- `PATH=/opt/homebrew/opt/openjdk/bin:$PATH npm run test:emulator` — **108/108** (18
  `packages/testing`, 86 `apps/functions`, 4 `apps/web`) against the local `demo-digitable`
  Auth/Firestore/RTDB/Functions emulators.
- `git diff --check` — clean; the only working-tree change from a fresh `npm install`
  (`package-lock.json` `"peer": true` metadata churn, the same immaterial diff the sonnet-w
  review already discarded) was reverted, not committed.

**Fresh live evidence, generated by this session (not just cited).** Built a from-scratch
emulator-mode production bundle (`VITE_FIREBASE_USE_EMULATOR=true`, a fake `demo-key`/
`demo-digitable` config — no real project, credential, or resource touched), started the local
Auth/Firestore/Functions emulators and served the build via `vite preview`, both bound to
`127.0.0.1` only (no LAN/network exposure, unlike `scripts/playtest/lan-up.sh`'s `0.0.0.0`
binding, which was not needed for this local audit). Ran:

```
node scripts/playtest/ui-audit.mjs --base http://127.0.0.1:4174 \
  --label sonnet-ad-independent --out <job-tmp>/ui-audit-out --port 9762 --no-shots
```

Result: `ok: true`, **150 states**, **1,434 controls audited** (the script's control selector is
`button, a[href], select, textarea, input, summary, [role="spinbutton"], [role="button"]` —
covers every native `<select>`, the `<details>/<summary>` disclosure, the custom
`role="spinbutton"` allocation stepper, and every button/link/input), **0 control issues**,
**0 overflow states**, **0 hard axe violations**, **0 failures**, **14/14 modal/sheet scenarios**
(phone-small/phone/phone-landscape/phone-landscape-667×375/tablet/desktop widths, plus
dynamic-viewport-keyboard emulation, pinch-zoom, safe-area insets, 200% text scaling, internal
scroll-to-control, and reduced-motion) each checked against all eight geometry/behavior
invariants (`dialogInsideViewport`, `actionsVisibleWithoutScrolling`, `noPageOverflow`,
`actionTargets44`, `reasonReachableAfterScroll`, `rootScrollLocked`, `backgroundInert`,
`focusInsideDialog`) plus the keyboard-emulated sub-checks (`dialogInsideViewport`,
`focusedFieldVisible`, `actionsVisible`, `noPageOverflow`) — every one of these `true` across
every scenario except the one already-documented, already-accepted 320px+200%-text extreme (see
below). Zero console errors or failed requests across gm/player/table/anon surfaces.
`reducedMotion` capture confirms the sheet's rise/fade animation genuinely collapses to
near-zero duration under `prefers-reduced-motion: reduce` (`sheetDuration` `0.18s` → `1e-06s`).
Background emulator/preview processes were stopped after the run; nothing was committed from
the emulator-mode build (`dist/` is gitignored).

**Pre-existing, already-documented, non-gating items reproduced (not new defects):**

- The 320px + 200%-text-scale combination for the correction sheet (scenario 13 of 14: only
  `dialogInsideViewport` and `noPageOverflow` read `false`) — the audit script itself logs this
  as `INFO ... (recorded, not gating)`, not a failure, matching the exact limitation
  `docs/reviews/2026-09-18-sonnet-d-reskin-independent-review.md` and every subsequent review
  already accepted as a documented geometry limit at that specific combined extreme, not a new
  finding.
- `anon/route-claim-no-such-room@phone: page-has-heading-one` — the same pre-existing axe
  best-practice (not hard-violation) note on the intentional nonexistent-room route, unchanged
  since the prior reviews.

**No other in-scope defect found; no code change made.** The pop-out system (`SheetDialog`, the
six native `<select>`s, the one `<details>` disclosure, the custom allocation stepper) is
unchanged since `930e3f1` and is confirmed — by fresh, from-this-session browser evidence, not
by citing prior runs — to satisfy every geometry, tap-target (44px+), focus-trap,
scroll-lock, and reduced-motion invariant this audit checks, at phone (320/375/812×375/tablet/
desktop/table widths), under dynamic-viewport-keyboard emulation, pinch-zoom, safe-area insets,
and 200% text scaling. Functional semantics, privacy/authorization invariants (platform
authorization before template authorization, per-viewer projection isolation, hidden-field
redaction), and every prior independent review's disposition are all unaffected — nothing in
`packages/*`, `templates/*`, `apps/functions`, `firestore.rules`, `database.rules.json`, or the
engine/authorization boundary was touched or needed to be. This is a documentation-only entry
(this file plus the `CLAUDE_HANDOFF.md` note it is linked from); no merge, deploy, or promotion
occurred.

**Physical-device limitation (unchanged, explicitly noted).** This review, like all prior
reskin passes, ran entirely in headless-adjacent (CDP, real Chrome binary) emulation on a
laptop — real iOS/Android hardware, real on-screen-keyboard resize behavior, and real Safari
visual-viewport quirks were not and could not be exercised here. That gap is already tracked as
open for John under `docs/PLAYTEST_TWO_DEVICE.md` and the existing physical-device evidence
item in `CLAUDE_HANDOFF.md`; this review does not close it and does not claim to.
