# S06 event-tail presentation: DeepSeek factory run, design review and independent review

- **Date:** 2026-09-18
- **Branch:** `factory/today-deepseek` (builds on `afd0ced`; not pushed, not merged)
- **Scope:** acceptance row 8 of `docs/PHASE_2_PLAN.md` (refresh must not re-fire an already-presented event) and the three code findings of `docs/reviews/2026-09-17-s06-outbox-reconciliation-review.md`
- **Seat provenance:** `docs/evidence/2026-09-18-deepseek-seats/README.md`
- **Verdict:** **approve with fixes** (independent review), and the fixes below were applied afterwards. The reviewer did **not** re-review those fixes; they are verified only by tests and mutation checks (see "Fixes applied after review"). Acceptance row 22 (physical devices) remains open, so this is not a claim that the milestone is finished.

## Two reviews, and what each was

1. **`deepseek-v4-pro` design review (`pro1`)** ran **before** implementation, against the design and the then-current tree. It is a design review, not a review of the code on this branch. Verdict: **AMEND**, 11 findings.
2. **Independent post-implementation review** by a fresh Claude subagent (general-purpose, read-only, given `AGENTS.md`, the uncommitted change set and specific questions). It is independent of the author and of the DeepSeek seats. Verdict: **APPROVE-WITH-FIXES**, no blockers, 5 medium and 3 low findings. It ran the redaction test, the web session/player2 tests and the template/engine projects (all passing); it did not run the Functions or emulator suites.

## Disposition of the `pro1` design findings

| #   | Sev     | Finding                                                            | Status                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Blocker | `readEventTail`/`readEventTailHead` missing from both repositories | Resolved in `afd0ced`. The in-memory repository keeps an append-only envelope log.                                                                                                                                                                                                                                                                                       |
| 2   | Blocker | `SceneEdited` shared copy leaks unrevealed Threat data             | Resolved. The same seat-E audit also found and fixed `RoundEnded` and `ActionResolved` shared copies.                                                                                                                                                                                                                                                                    |
| 3   | High    | Bounded ack list evicts ids, so events re-present                  | Resolved differently from the amendment. The cursor stays below the oldest pending event, acknowledged ids are a 512-entry FIFO, and an unacknowledged event more than 300 sequences behind the newest is expired, so an evicted id can never be re-read above the cursor (invariant in the `presentationLedger.ts` header, plus a property test). Cost: such an event is never presented. |
| 4   | High    | Baseline-at-head hides the member's own recovered `ActionResolved` | Resolved for receipts that carry `acceptedSequence` (tests d2, d3, d4). Residual: see independent finding 2.                                                                                                                                                                                                                                                             |
| 5   | Medium  | Cross-partition merge not globally ascending                       | Resolved (full-partition watermark in `eventTail.ts`; property test).                                                                                                                                                                                                                                                                                                     |
| 6   | Medium  | Shared `ActionResolved` exposes injury detail the projection hides | **Open.** Product decision, see "Open items".                                                                                                                                                                                                                                                                                                                             |
| 7   | Medium  | 32-entry roll-context FIFO can evict the in-flight roll            | **Partial.** Cap is 64, still FIFO, no pinning of the seat's own in-flight roll. See independent finding 7.                                                                                                                                                                                                                                                              |
| 8   | Medium  | Multi-tab ledger races                                             | **Partial.** Merge-on-save (cursor max, acknowledged-id union) prevents cursor regression; two tabs may each present a summary once. Documented in the runbook.                                                                                                                                                                                                          |
| 9   | Low     | Revision gate can stall on a stale cached projection               | Mitigated. The gate is derived (`useMemo`), so a projection snapshot releases already-published items without another sync; the dispatch-path delay is fixed (independent finding 1).                                                                                                                                                                                     |
| 10  | Low     | StrictMode double-mount                                            | No defect found. Cleanup drops the session and the next mount reloads the persisted ledger (tests c, d). No in-tab presented set was added.                                                                                                                                                                                                                               |
| 11  | Low     | Shared `ActionDeclared` carries `actorMemberId`                    | **Open**, report-only (a member id is not GM-only, same class as `CharacterClaimed.memberId`).                                                                                                                                                                                                                                                                           |

## Disposition of the independent review findings

| #   | Sev    | Finding                                                                                                                                                                | Status                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Medium | A sync requested during an in-flight one was dropped; the running pass then reset the backoff to 30 s, so the resolution summary could arrive ~30 s after a dispatch    | **Fixed.** `resync` flag; test `h` (fails when the flag is removed).                                                                                                                                                                                                                                                                                                      |
| 2   | Medium | Recovered sequences were recorded only after a refresh that can throw, so a failed refresh baselined the recovered event away                                          | **Fixed** for receipts carrying `acceptedSequence` (recorded immediately after `reconcilePending`; test `d4`, fails when moved back). **Open residual:** the resend path returns a callable result with no `acceptedSequence` (`gameCommandAuthority.ts` puts it only in the receipt), so with no ledger yet a recovered summary can still be baselined away. Needs a callable-contract change. |
| 3   | Medium | After an injury choice the summary showed a misleading "Unresolved opposition" line, and an early Continue left the `InjuryCategoryChosen` copy unacknowledged forever | **Fixed.** The line is not shown while an injury choice is pending, and `isHeldForResolution` releases an own `InjuryCategoryChosen` that has no queued `ActionResolved`. Tests in `presentationQueue.test.ts` and `ConfirmSummary2.test.tsx`.                                                                                                                             |
| 4   | Medium | The ledger dedupes by `eventId`, but the engine reuses some ids (`session-paused`, `mission-ended`, `round-N-ended` after a round reset, `scene-edited-N`)              | **Open follow-up.** Not re-verified by the author. Harmless to the dashboard today (it consumes only roll-scoped `ActionResolved`/`InjuryCategoryChosen`); a later event with a reused id would be silently dropped by any future timeline/theatre consumer.                                                                                                           |
| 5   | Medium | Pre-existing: `AllocateResults` accepts an active but unrevealed Threat target, so a player can mutate it and probe whether a guessed id exists                        | **Open, separate change.** Also observed by seat E. The shared-copy redaction fixes the leak in events but not the rule; closing it changes accepted commands and needs its own decision and test.                                                                                                                                                                         |
| 6   | Low    | Blocked `localStorage` silently disables presentation                                                                                                                  | Open, accepted. The command outbox also refuses to dispatch when storage is blocked, so play does not continue anyway.                                                                                                                                                                                                                                                    |
| 7   | Low    | `attackSuccessesRolled` is unavailable when the ledger is created after `ActionRolled` (first visit, cleared storage, second device joining mid-roll)                  | Open. The "Defended" line is omitted in that case; no wrong number is shown.                                                                                                                                                                                                                                                                                             |
| 8   | Low    | Test quality                                                                                                                                                           | **Partly fixed.** Assertions (a), (f), (g) tightened, an invalid d3 face corrected, and an assertion added that the GM and shared copies are one logical event. Not done: an `attackBumpThreatId` case; the "dismissed" case in `recoveredResolution.test.ts` remains trivially true.                                                                                     |

## Fixes applied after review

Web hook, queue helper and summary component (independent findings 1, 2, 3 and 8), each with a test. For the two hook fixes (findings 1 and 2) the new test was confirmed to fail with the bug reintroduced and pass with the fix. No one other than the author has looked at these changes since the review.

## Open items (not resolved on this branch)

1. **Acceptance row 22:** no physical-device or staging evidence. `docs/TWO_DEVICE_RUNBOOK.md` has a blank evidence table and states that LAN devices are not supported by the current code (hard-coded loopback emulator endpoints, no LAN emulator config, `crypto.randomUUID()` unavailable on a plain-HTTP LAN origin).
2. **Player-private detail in shared event copies (pro1 #6, #11; seat E report-only list):** shared `ActionResolved`/`InjuryCategoryChosen` carry the acting character's full injury mark; shared `ActionDeclared` carries `actorMemberId`; also `InjuryHealed`, `ActionRolled.approvedBonusClaims`, `CharacterCorrected.patch`, `RollVoided.itemRestoreDeltas`, `ItemGranted.item`, `AdvanceUnlocked.advanceId`. None names an unrevealed Threat. Whether other players may read these is a product decision for John; redacting the acting player's own `ActionResolved` would also need a member-partition copy for their summary.
3. Independent findings 2 (resend path), 4, 5, 6 and 7 above.
4. A second independent pass over the post-review fixes.
