# Reskin verification (sonnet-o, 2026-09-24, hourly)

Branch `sonnet-o/reskin-hourly-20260924`, isolated worktree `/private/tmp/digitable-sonnet-reskin-hourly-20260924-fresh`, HEAD `41984c7` throughout this pass. Another fresh, independent audit of the same ink-black mobile-hardening reskin candidate that `sonnet-h` (2026-09-20), `sonnet-k` (2026-09-22 and 2026-09-23, two passes), and `sonnet-m` (2026-09-23, hourly) already reviewed and found no defect in. This lane's brief was to independently re-inspect every pop-out/select/menu/disclosure/modal/drawer/action/allocation control at phone/tablet/desktop/table sizes under visual-viewport, keyboard, and safe-area conditions, and to actually re-run the gates and a fresh real-Chrome audit rather than cite prior results.

- **Question:** on a from-scratch worktree (`npm install` had never run here), does this candidate still hold up under a genuinely independent re-inspection of source plus the full gate suite plus a fresh real-Chrome accessibility/responsive audit? **Answer:** yes, no concrete defect found.
- **Source confirmation:** `git diff --stat c180820 HEAD -- . ':!docs'` touches only `CLAUDE_HANDOFF.md` — the last source-touching commit remains `c180820`; nothing has changed in `apps/web/src` (or anywhere else outside `docs/`) since every prior reviewed pass.
- **Independent inventory (grepped fresh, not copied from docs):** exactly six native `<select>`s (two in `SceneDirector.tsx`, four in `GmToolsPanel.tsx`), one `<details>` disclosure (`ComposeStep2.tsx`'s "Why?" pool breakdown), one `SheetDialog` (single consumer `CorrectionDialog.tsx`), and one `AllocationStepper` — matches the inventory every prior pass recorded; nothing new exists.
- **Source read:** `SheetDialog.tsx` (portal + inert background + focus trap's `isReachable` collapsed-`<details>`/`inert`/disabled-fieldset/radio-group handling + Escape/Tab cycling + visual-viewport-aware keyboard reveal), `useVisualViewportBox.ts` (the `differs` divergence check and `--vv-*` write/clear), `CorrectionDialog.tsx` end to end (steppers, fieldsets, live-region Blood preview, required-reason gating), `AllocationStepper.tsx` (spinbutton role, Arrow/Home/End keyboard handling, clamp behavior), and every native `<select>` call site in `GmToolsPanel.tsx`/`SceneDirector.tsx` (each has its own `<label htmlFor>`). No new mobile-viewport, focus, keyboard, touch-target, or accessibility defect found.
- **Commands and results:** `npm install` (clean; this worktree had no `node_modules`). `npm run check` exit 0 (**708 passed, 11 todo**, 75 files passed, 1 skipped — matches the baseline on every prior pass exactly). `npm run build` exit 0 (existing chunk-size warning only). `PATH=/opt/homebrew/opt/openjdk/bin:$PATH npm run test:emulator` exit 0 (**18** `packages/testing`, **86** `apps/functions`, **4** `apps/web`).
- **Fresh real-Chrome UI audit:** built a scratch `vite build` (`VITE_FIREBASE_USE_EMULATOR=true`), started local Firebase emulators (auth/firestore/functions/database, project `demo-digitable`, ports 9099/8080/9000/5001 — all free at run time), served the build with `vite preview` on a free local port, and ran `node scripts/playtest/ui-audit.mjs` against it end to end (create/join/recover/claim/opposed-action/pause-resume/scene-advance/GM-correction-sheet flows across `phone-small`/`phone`/`phone-390`/`phone-landscape`/`tablet`/`desktop`/`table` viewports). **PASSED**: 189 states, 1,820 controls audited, **0 control issues, 0 overflow states, 0 axe hard violations**, 0 console errors and 0 failed requests on all four surfaces (`anon`/`gm`/`player`/`table`), 6/6 roster-sheet checks at both audited viewports, all 3 GM-correction-sheet geometry/keyboard/focus-restore checks true at every audited viewport, keyboard hints (`autocapitalize`/`autocorrect`/`spellcheck`) correct on room-code/passphrase/recovery-code fields, `prefers-reduced-motion` correctly suppresses the sheet's rise/fade animation. The only non-gating notes, both already documented by every prior pass: `page-has-heading-one` on the intentional nonexistent-room route, and the known 320px/200%-text modal edge case. Evidence (report.json + 208 screenshots) is in this directory.
- **Visual spot-check (informal, supplementary):** opened the GM correction sheet (phone) and the anon landing screen (desktop) screenshots directly. Both show the near-black ground, bold condensed uppercase display type with hard drop shadow, and neon red/cyan/acid-yellow accents the design brief calls for, with legible light text throughout.
- **No source change was made** — nothing concrete to fix means nothing to regress-test. Per the `sonnet-k`/`sonnet-m` precedent, no new `docs/reviews/` record was created for this no-op confirmation (the review record from `docs/reviews/2026-09-20-uiux-reskin-hardening-review.md` and the subsequent independent passes remains the operative review for this unchanged source).
- **Explicit limits (unchanged from every prior pass):** all evidence is headless Chrome on one machine with emulated touch/keyboard/safe-area conditions. No physical iOS/Android device rehearsal, no VoiceOver/TalkBack/NVDA pass. Not deployed; staging Hosting (`https://digitable.signal-bleed.com`) still serves the pre-reskin build. These remain open for John, not newly blocking.
- **Next action for this lane:** unchanged — John reviews/merges draft PR #39, decides on a Hosting redeploy, then completes the physical-device rehearsal (iPhone keyboard-up pass on the GM correction sheet and recovery form; VoiceOver pass over compose utility rows, the injury choice, and recovery).
